---
title: Changelog
description: What changed in each release of hypr-swap, and what broke.
---

The repository's `CHANGELOG.md`, included here rather than restated, so the page a reader upgrading
lands on and the file the release workflow writes are the same bytes (FR-084). The site itself
documents [`master`](./index.md) — the latest release plus anything merged since — which is why what
belongs to which version is answered here and nowhere else.

::include[../CHANGELOG.md]
